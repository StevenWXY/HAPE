# Clipli 白皮书

> **品牌释义：** Create、Click、Grow

> **版本：** v0.3 External Data & Treasury Draft
> **状态：** 内部审阅稿  
> **日期：** 2026-08-18
> **适用范围：** Clipli、HAPW、创作额度、CLIP 及资产行权流程
> **语言：** 简体中文

## 阅读声明

本文件用于说明 Clipli 的产品设计、机制参数、技术方向和已知边界。除非后续正式版本另有明确披露，本文件中的数值、流程、流动性池、资产价格、作品和任务均为原型或演示设计，不构成真实交易报价、代币发行要约、证券发行、收益承诺、投资建议、法律意见、税务意见或版权权属证明。

Clipli 不托管用户私钥、助记词或钱包资产。任何与钱包、区块链、DEX 或第三方平台有关的实际操作，均须以用户钱包确认、正式合约、适用法律和第三方规则为准。HAPW 的持有、核销或资产行权不自动转移底层版权；CLIP 亦不代表作品所有权、固定收益权或法币兑付权。

本文使用“当前原型”描述已经在演示产品中实现的行为，使用“正式网络前”描述仍需经技术、业务与法律确认的事项。若创作额度与 CLIP 记录存在差异，以经版本化披露的正式规则为准。

## BNB 合约补充（2026-09-02）

本稿中关于固定总量、无增发和 Ethereum/未定网络的旧描述由 [docs/BNB_TOKEN.md](docs/BNB_TOKEN.md) 的 BNB Smart Chain 方案取代：CLIP 硬顶为 1,000,000,000，部署时铸造 900,000,000，最多 100,000,000 由带指标快照、Timelock 延迟、Guardian 取消和年度上限的 `AdaptiveMinter` 释放。`ClipAirdrop` 使用资产快照 Merkle root，DEX 生产兑换应接入 PancakeSwap 等经审计的 BNB 部署；本仓库的 `ClipliDexPair` 只用于本地测试。

---

## 目录

0. [审阅重点](#0-审阅重点)
1. [文档信息与版本管理](#1-文档信息与版本管理)
2. [执行摘要](#2-执行摘要)
3. [行业背景与问题](#3-行业背景与问题)
4. [设计原则与边界](#4-设计原则与边界)
5. [生态参与者](#5-生态参与者)
6. [HAPW：版权素材核心资产](#6-hapw版权素材核心资产)
7. [创作额度](#7-ai-创作额度)
8. [CLIP：创作服务积分（代币）](#8-clip创作服务积分代币)
9. [DEX 与 CLIP/USDT](#9-dex-与-clipusdt)
10. [CLIP 到 HAPW 的储备兑换](#10-clip-到-hapw-的储备兑换)
11. [AI 视频任务与内容记录](#11-ai-视频任务与内容记录)
12. [第三方平台与外部入口](#12-第三方平台与外部入口)
13. [账本与可追溯性](#13-账本与可追溯性)
14. [技术架构](#14-技术架构)
15. [安全、隐私与内容治理](#15-安全隐私与内容治理)
16. [法律与合规](#16-法律与合规)
17. [治理与参数变更](#17-治理与参数变更)
18. [商业模式](#18-商业模式)
19. [路线图](#19-路线图)
20. [风险披露](#20-风险披露)
21. [附录](#21-附录)

---

## 0. 审阅重点

本版作为完整白皮书的第一份内部审阅稿，优先确认以下事项。尚未确认的项目不应被产品界面、营销材料或外部沟通描述为正式规则：

1. HAPW 核销在正式网络中的状态究竟是销毁、锁定还是转入不可用地址。
2. HAPW 授权期限、可改编范围、暂停和撤销条件的统一模板。
3. 每核销额度发放 `0.40 CLIP`、每创作额度消耗 `0.20 CLIP` 是否作为首发参数。
4. CLIP 的目标网络、合约标准、精度、一次性铸造记录、平台金库权限和分配记录公开方式。
5. CLIP/USDT 流动性池的资金来源、池地址、做市职责和风险披露。
6. 储备 HAPW 的来源、定价权、5% 手续费账户、盘点频率和审计规则。
7. 资产行权与底层版权转让、许可、链上资产状态之间的法律边界。
8. 海文发、OpenSea、Foundation、SuperRare、Art Blocks 等第三方平台的准入、费用、审核和责任边界。
9. 生成内容审核、侵权申诉、下架、任务失败和退款的正式规则。

---

## 1. 文档信息与版本管理

### 1.1 封面与项目定义

Clipli 的名称取意于 **Create、Click、Grow**：从有来源的素材开始创作，用明确动作连接授权、资源与服务，再让作品和使用记录持续扩展。它是一个面向版权素材、AI 视频生成和外部发布场景的记录与操作平台，尝试将以下原本分散的信息放入同一条可核对的工作链路中：

- 素材由谁授权；
- 素材可被怎样改编、在何处使用、有效期为何；
- 哪一枚 HAPW 对应哪一份素材与授权边界；
- 用户何时核销 HAPW，并获得多少创作额度和 CLIP；
- 某次 AI 视频生成实际消耗了多少额度和 CLIP；
- 未核销的 HAPW 向哪一个第三方平台发起了资产行权。

Clipli 的首要目标不是将作品抽象成脱离上下文的数字编号，而是在作品、授权、素材使用、费用和外部入口之间保留可追溯关系。

### 1.2 版本记录

| 版本 | 日期 | 状态 | 主要内容 |
| --- | --- | --- | --- |
| v0.1 Draft | 2026-08-10 | 内部审阅 | 首次按产品原型整理授权、生成、DEX、储备兑换、资产行权和风险边界 |
| v0.2 Rebrand Draft | 2026-08-14 | 内部审阅 | 项目更名为 Clipli；明确 Create、Click、Grow 品牌含义，HAPW 定义为 IP 授权证书，创作额度与 CLIP 使用统一命名 |
| v0.3 External Data & Treasury Draft | 2026-08-18 | 内部审阅 | 明确 HAPW 数据来自外部平台；CLIP 固定总量一次性铸造、平台金库中心化分配；原型采用匿名会话与 Go API |

后续版本至少应记录以下变化：机制参数、合约地址、网络选择、HAPW 授权模板、CLIP 金库分配规则、DEX 池地址、储备审计方法、手续费去向、第三方平台范围及法律声明。所有重大变化应标注生效时间和适用对象，且不得静默改写已经完成的账本记录。

### 1.3 阅读声明

本白皮书应与正式版权授权合同、产品服务条款、隐私政策、合约说明和风险提示共同阅读。原型参数用于解释机制，不代表正式合约已经部署，也不代表平台承诺维持该参数。涉及权利、费用、资产或外部平台的实际操作，应以操作发生时展示并由用户确认的有效版本为准。

---

## 2. 执行摘要

### 2.1 一句话定义

Clipli 将版权持有方授权的素材封装为 HAPW；用户核销 HAPW 后，同时获得与该素材绑定的创作额度和 CLIP，并在生成 AI 视频时共同消耗二者。

### 2.2 核心闭环

```text
版权方确认素材与边界
        ↓
发行 / 持有 HAPW
        ↓
核销 HAPW（不可重复）
        ↓
创作额度 + CLIP 同时入账
        ↓
按时长与质量共同消耗，创建 AI 视频任务
        ↓
记录任务、版本、内容表现与外部操作状态
```

该闭环之外存在两条独立路径：

- 用户可在外部 DEX 使用 USDT 获取 CLIP，实际汇率由外部 DEX 与钱包报价决定。
- 用户可用 CLIP 支付已披露价格与手续费，从有限储备中兑换尚未核销的 HAPW；未核销 HAPW 亦可向指定第三方平台发起资产行权。

### 2.3 核心价值

Clipli 用三类彼此分离、相互引用的记录回应 AI 视频生产中的实际问题：

- **HAPW** 是面向不同 IP 的授权证书资产，记录“是否具有使用某项版权素材的资格”；
- **创作额度** 记录“可使用多少与该授权绑定的生成资源”；
- **CLIP** 记录“生成服务和生态服务的费用与获取路径”。

这种分账设计避免由一个资产同时承担版权、计算资源、服务费和金融回报等相互冲突的含义。内容播放、完成度和渠道表现可以被记录，用于复盘、运营分析和渠道比较，但不会被定义为 CLIP 发放或固定收益的依据。

---

## 3. 行业背景与问题

### 3.1 AI 视频生产正在改变内容再创作的方式

AI 视频工具降低了镜头生成、角色动画、声音设计、本地化和短视频改编的生产门槛。一份经授权的角色、场景、叙事或声音素材，能够被延展为多个版本、多个语言和多个区域渠道的内容。

生产能力提升并不会自动解决权利问题。内容团队仍须回答：素材来自哪里、谁可以允许改编、哪些元素不可使用、生成版本是否超出地域或渠道限制、外部平台是否接受该内容、费用由谁承担。这些信息若散落在聊天记录、合同附件、表格和钱包地址中，后续很难复核。

### 3.2 当前痛点

**第一，授权来源与实际使用脱节。** 素材可能有合同，但生成任务无法直观看到对应的权利人、授权范围、期限或限制。

**第二，素材资格与计算成本混在一起。** 获得素材的资格、生成资源消耗和平台服务费用经常采用同一个模糊口径，导致用户无法判断每次生成到底消耗了什么。

**第三，跨平台操作缺少共同记录。** NFT 市场、数字艺术平台、文化发行平台各有账户、网络、审核和收费规则；用户在平台之间跳转后，原始素材、授权与操作状态难以关联。

**第四，机制语言容易被误读。** 若将播放表现、内容数据、资产价值和代币发放放在同一叙事中，用户可能误以为平台承诺收益或价格。Clipli 因此将播放数据定位为内容表现记录，而将 CLIP 的发放与消耗规则单独公开。

### 3.3 Clipli 的解决路径

Clipli 不试图代替版权合同、司法判断或第三方平台审核，而是建立一套可追溯的操作层：

1. 用 HAPW 把素材包及其使用资格定位到可识别资产。
2. 用核销凭证保存一次素材使用资格被开启时的授权快照。
3. 用创作额度记录计算资源，用 CLIP 流水记录服务成本。
4. 用资产行权记录承接用户前往第三方平台的动作。
5. 用任务、版本和内容表现记录支持后续复盘，而不把表现数据写成收益承诺。

---

## 4. 设计原则与边界

### 4.1 设计原则

**先确认权利，再开放素材。** 用户在 AI 工作室中使用某项素材之前，系统应能定位相应 HAPW、权利人和授权范围。

**核销发放必须可复核。** 每一次核销需关联唯一 HAPW、唯一操作标识、授权凭证、初始额度和 CLIP 发放结果。

**生成成本必须完整扣除。** 创建任务时，服务端应独立计算创作额度与 CLIP 消耗，只有额度与 CLIP同时充足时才创建任务。

**操作状态必须可追溯。** 行权目标、签名状态、储备兑换、生成任务和外部入口都应保存时间、参数、结果和关联资产。

**演示数据与正式数据必须分开。** 演示汇率、流动性、资产价格或生成任务不能被表述为实时市场报价、已部署合约状态或收益承诺。

### 4.2 Clipli 负责的事项

- 保存 HAPW 与素材授权相关的展示性元数据和引用关系；
- 创建、查询与审计核销凭证、创作额度、CLIP 收支、任务和资产行权记录；
- 在操作前披露公式、费用、参数快照和确认信息；
- 为外部平台提供选择器、链接和可追溯的行权状态；
- 在正式网络前持续披露机制与风险边界。

### 4.3 Clipli 不负责的事项

- 不替代正式版权合同、权属调查、司法裁决或专业法律意见；
- 不托管私钥、助记词或用户钱包资产；
- 不保证 CLIP 价格、流动性、DEX 成交、内容传播范围或第三方上架结果；
- 不将 CLIP 余额视为 HAPW 权利，也不允许仅凭 CLIP 绕过素材资格；
- 不以渠道数据、内容传播或任何价格预期承诺 CLIP、法币或固定回报。

---

## 5. 生态参与者

### 5.1 版权持有方

版权持有方提供素材包及权属、授权范围和限制条件。其职责包括确认可改编元素、地域与渠道限制、授权期限、禁止事项以及可能的暂停或撤销条件。

在 Clipli 的设计中，版权持有方授权 HAPW 并不意味着向平台或持有人自动转让底层版权。HAPW 是对特定使用资格的可记录载体，正式合同应明确其与著作权、邻接权、商标、肖像、声音、角色和衍生创作权之间的关系。

### 5.2 HAPW 持有人与创作者

HAPW 持有人在核销前应核对素材、权利人、授权范围、额度产出与外部行权限制。核销后，持有人可在对应范围内调用 AI 生成服务，并应对输入、输出、发布和外部平台操作承担相应责任。

创作者可管理创作额度与 CLIP，选择视频时长和质量，并查看任务状态与内容表现。创作者不应将 HAPW、CLIP 或外部平台入口理解为对内容审核、版权适用性或商业结果的保证。

### 5.3 Clipli 平台

Clipli 维护不同实体之间的引用关系，并在当前原型中实现幂等核销、创作额度与 CLIP 发放、生成费用扣除、储备兑换和资产行权请求。平台应确保规则可读、参数可见、操作需确认、失败有明确状态。

### 5.4 第三方平台

当前产品入口包括海文发、OpenSea、Foundation、SuperRare 和 Art Blocks。它们分别代表不同的数字文化发行、NFT 市场、数字艺术和链上生成艺术场景。

Clipli 仅提供外部入口和本地操作记录，不代表对任何第三方平台的背书，也不保证其准入、上架、审核、交易、下架或内容处理结果。用户进入第三方平台后，应自行确认账户资格、网络、合约、费用、许可条款和当地规则。

### 5.5 技术服务方

正式系统可能接入 AI 视频模型、内容审核服务、钱包连接器、区块链节点、DEX 报价源、数据索引服务、对象存储和外部平台适配器。每个服务方的可用性、数据使用和责任边界均需以独立协议与技术文档说明。

---

## 6. HAPW：版权素材核心资产

### 6.1 定义与用途

HAPW 是 Clipli 的核心资产类型，用于指向由版权持有方授权的具体素材包及其使用边界。一个 HAPW 可关联作品、角色、场景、声音设定、叙事元素或其他可被明确描述的素材集合。

HAPW 的作用是表明持有人可以在两条互斥路径中选择一条：

1. **核销**：在 Clipli 内不可逆地开启绑定素材的 AI 生成许可，并取得创作额度和 CLIP。
2. **资产行权**：保留 HAPW 未核销状态，向选定第三方平台创建可追溯的签名请求；该操作本身不产生创作额度或 CLIP。

持有 HAPW 不等于获得全部底层版权；核销 HAPW 也不应被解读为无条件、无限期、全地域或可任意再授权的素材许可。具体权利始终受对应授权文本、元数据、期限和适用法律限制。

### 6.2 建议元数据

正式网络中的 HAPW 至少应关联以下字段或其不可篡改摘要：

| 类别 | 建议字段 | 作用 |
| --- | --- | --- |
| 标识 | HAPW ID、链上标识、版本 | 区分资产与授权版本 |
| 素材 | 作品、角色、场景、声音、素材清单摘要 | 定义可被使用的对象 |
| 权属 | 版权持有方、证明摘要、授权状态 | 支持权利核对 |
| 范围 | 可改编形式、地域、渠道、语言、期限、禁止事项 | 限制使用边界 |
| 生成 | 标注额度 `Y`、可选质量或模型限制 | 计算生成资源 |
| 储备 | 是否可兑换、公开 CLIP 价格、领取状态 | 管理有限储备 |
| 状态 | 可核销、已核销、争议、暂停、已提交行权 | 限制可执行操作 |

版权文件、个人信息和未公开商业条款不宜直接上链。正式架构可存储文件哈希、签名摘要、版本号和受控链下凭证，以兼顾可核对性与保密需求。

### 6.3 生命周期

HAPW 的建议状态机如下：

```text
待授权复核
    ↓
可持有 / 可核销 / 可行权
    ├── 资产行权请求 → 等待签名 / 已提交 / 外部回执
    └── 核销成功 → 已核销（不可再次行权或核销）

任意阶段：争议或暂停
```

在当前原型中，核销成功后资产会变为不可再次核销、不可再次行权的状态。正式链上实现应明确其状态承载方式，并对争议、误操作、撤销和合约升级设计清晰的治理与法律流程。

### 6.4 核销

核销是将 HAPW 的素材使用资格转入一次性、可审计的生成授权凭证的操作。一次有效核销至少应保存：

- HAPW 标识和当时的权利快照；
- 操作发起方、时间和唯一 `requestId`；
- 生成授权凭证编号；
- 初始创作额度 `Y` 与剩余额度；
- 同步发放的 CLIP 数量 `G`；
- 使用的机制版本与参数快照。

核销不可重复。当前原型将资产标记为已核销；正式网络须在用户确认前清楚展示其不可逆性质及最终链上状态。

### 6.5 资产行权

资产行权是指 HAPW 持有人将可执行的资产操作关联到某一外部平台，并创建钱包签名请求的过程。它不是自动上架、自动交易、版权转让或平台接纳。

资产行权只应对未核销、未冻结且满足授权条件的 HAPW 开放。记录中应包含资产标识、目标平台、操作时间、签名状态、外部资产标识或回执（如有）以及失败原因。若发生核销、争议或授权暂停，平台应阻止或终止后续行权流程。

---

## 7. 创作额度

### 7.1 定义

创作额度是由 HAPW 核销产生、并绑定具体授权凭证与素材范围的计算配额。它不是独立的可自由流转资产，也不应被用于未关联 HAPW 的素材。

额度的设计目标是让用户在创建任务前能够理解生成资源消耗，并让平台在任务账本中保留可复核的成本记录。

### 7.2 额度消耗公式

设：

- `S` 为视频秒数；
- `Q` 为质量系数；
- `C` 为本次任务消耗的创作额度。

则：

```text
C = ceil(S × Q)
```

当前原型参数：

| 质量 | 分辨率示例 | `Q` |
| --- | --- | ---: |
| 标准 | 720p | 1.0 |
| 高质量 | 1080p | 1.5 |

`ceil` 表示向上取整，确保每次生成对应整数账本扣减。

### 7.3 当前原型示例

| 配置 | 计算 | 额度消耗 `C` |
| --- | --- | ---: |
| 15 秒标准 | `ceil(15 × 1.0)` | 15 |
| 15 秒高质量 | `ceil(15 × 1.5)` | 23 |
| 30 秒高质量 | `ceil(30 × 1.5)` | 45 |
| 60 秒高质量 | `ceil(60 × 1.5)` | 90 |

### 7.4 一致性与失败处理

前端可以向用户展示预估值，但正式服务端必须重新计算成本，不能直接信任客户端。服务端需同时检查总额度余额与单一授权凭证剩余额度；任一不足时，不得创建任务，也不得发生部分扣款。

当前原型实现了生成前校验，以及创作额度与 CLIP 的同步扣减。正式系统还需定义任务排队、模型失败、审核拒绝、超时中止、重复提交和退款的状态机。退款是否全额、部分或不适用，应按任务实际阶段、模型供应商成本和适用合同公开说明。

---

## 8. CLIP：创作服务积分（代币）

### 8.1 定义

CLIP 是 Clipli 使用的创作服务积分（代币）。CLIP 采用固定总量：在正式部署时一次性铸造全部供应，初始进入平台控制的钱包，不提供持续增发或面向用户的铸造入口。平台依据公开、版本化的中心化规则从金库向用户账本分配 CLIP；用户也可通过外部 DEX 获取。创建 AI 视频任务时，CLIP 与创作额度共同消耗。

CLIP 不代表 HAPW、底层版权、作品股权、固定回购、价格保护或收益权。持有 CLIP 不会自动获得任何素材使用资格。

### 8.2 固定供应与金库守恒

当前原型用中心化账本模拟一次性铸造与分配。设：

- `M` 为一次性铸造总量；
- `T` 为平台金库可分配余额；
- `L` 为配置到外部 DEX 流动性池的数量；
- `B` 为用户与业务账本中的流通余额。

任意有效状态都应满足：

```text
M = T + L + B
```

当前演示快照如下：

| 项目 | 数量 |
| --- | ---: |
| 一次性铸造总量 `M` | 10,000,000 CLIP |
| 平台金库 `T` | 9,746,540 CLIP |
| DEX 流动性配置 `L` | 250,000 CLIP |
| 用户与业务账本 `B` | 3,460 CLIP |

核销发放时，平台金库减少 `G`、用户账本增加 `G`；生成、区域服务或储备兑换产生的 CLIP 费用进入平台金库。每笔变化都必须带唯一 `requestId` 与分配规则版本，守恒校验失败时不得提交状态。

### 8.3 核销发放

设：

- `Y` 为 HAPW 标注的创作额度；
- `G` 为核销成功时一次性记入的 CLIP。

当前原型使用：

```text
G = floor(Y × 0.40)
```

| HAPW 标注额度 `Y` | 计算 | CLIP 发放 `G` |
| ---: | --- | ---: |
| 90 | `floor(90 × 0.40)` | 36 |
| 100 | `floor(100 × 0.40)` | 40 |
| 120 | `floor(120 × 0.40)` | 48 |
| 150 | `floor(150 × 0.40)` | 60 |

每次发放都应关联唯一的核销凭证。相同 `requestId` 的重试应返回第一次结果，而不能重复记账。

### 8.4 生成消耗

设：

- `C` 为本次任务的创作额度消耗；
- `P` 为本次任务同时消耗的 CLIP points。

当前原型使用：

```text
P = ceil(C × 0.20)
```

| 配置 | 创作额度 `C` | CLIP `P` |
| --- | ---: | ---: |
| 15 秒标准 | 15 | 3 |
| 15 秒高质量 | 23 | 5 |
| 30 秒高质量 | 45 | 9 |
| 60 秒高质量 | 90 | 18 |

当额度或 CLIP 任一余额不足时，系统必须拒绝任务。额度与 CLIP在同一生成操作中扣除，并分别留下额度与 CLIP 账本记录。

### 8.5 分配边界

CLIP 不设追加铸造接口。当前原型唯一的用户分配来源是 HAPW 核销，平台不能通过修改业务参数突破固定总量。若未来增加任务预算、合作方额度或其他分配场景，必须从既有金库余额中划拨，并先披露规则、负责人、额度上限、适用范围、生效版本和历史记录。

### 8.6 使用场景

CLIP 的设计使用场景包括：

- AI 视频生成服务费用；
- 区域授权与本地化服务费用；
- 兑换有限储备的 HAPW；
- 在外部 DEX 与 USDT 等资产进行市场交换。

每个场景均应独立显示费用、输入资产、执行条件、失败状态与外部依赖，避免将服务费、资产权利和市场价格混为一体。

---

## 9. DEX 与 CLIP/USDT

### 9.1 获取 CLIP 的方式

用户可以在外部 DEX 上选择 CLIP/USDT 交易对，并通过自己的钱包完成交易。建议操作顺序如下：

1. 连接支持目标网络的钱包。
2. 在钱包与 DEX 中确认正式 CLIP 合约地址。
3. 核对池地址、输入资产、滑点、网络费用和最小接收量。
4. 以钱包最终报价为准确认交易。
5. 等待链上确认后，在 Clipli 中查看与业务相关的后续操作。

在正式网络、合约地址、链 ID 和池地址未公开前，Clipli 不应仅根据“名称相同”的代币引导用户兑换。

### 9.2 当前原型演示池

当前产品使用以下静态演示快照说明双边池信息：

| 参数 | 演示值 |
| --- | ---: |
| CLIP 储备 | 250,000 CLIP |
| USDT 储备 | 25,000 USDT |
| 参考比例 | 1 USDT 约等于 10 CLIP |
| 反向参考比例 | 1 CLIP 约等于 0.10 USDT |
| 双边名义流动性 | 约 50,000 USDT |

在该示例中，双边名义流动性按 `USDT 储备 × 2` 计算，只用于界面说明，并不表示实际可成交深度、真实价格、滑点或任何流动性承诺。

### 9.3 风险披露

CLIP 不固定锚定 USDT。实际兑换价格可能受池储备、交易规模、滑点、市场波动、矿工费、路由、合约风险和流动性撤出影响。Clipli 当前不托管 DEX 资金，也不承诺成交价格、持续流动性或价格稳定。

正式上线前应公开可信的合约地址、目标网络、池地址、初始流动性来源、管理权限、报价延迟和用户风险提示。

---

## 10. CLIP 到 HAPW 的储备兑换

### 10.1 设计目的

储备兑换为 CLIP 提供生态内的明确使用路径：用户可从有限的、尚未领取的 HAPW 储备中选择一项资产，并以公开 CLIP 价格和手续费完成兑换。

该路径不是无限回购、固定价格承诺或自动套利机制。储备 HAPW 的来源、数量、价格、可领取状态与费用归属均需要可审计披露。

### 10.2 报价公式

设：

- `P` 为某一储备 HAPW 的公开 CLIP 价格；
- `r` 为手续费率；
- `T` 为用户需要支付的 CLIP 总额。

当前原型使用 `r = 5%`：

```text
T = P + ceil(P × r)
```

例如某项 HAPW 储备价格为 `520 CLIP`，手续费为 `ceil(520 × 5%) = 26 CLIP`，总消耗为 `546 CLIP`。

### 10.3 每日限额与有限库存

储备 HAPW 是有限库存。当前原型对每个账户设置每日最多 `2` 枚的兑换上限，以 UTC 自然日为统计周期，并按服务端记录的已完成兑换计数；待处理、失败或重复请求不占用额度。达到上限后，接口返回 `hapw_exchange_daily_limit`，前端停止继续提交，也不会扣除 CLIP。

每日限额和库存是两道独立约束：即使当日额度仍有剩余，某个储备份额已经领取或库存归零也不能兑换；反过来，库存充足也不能绕过账户每日上限。正式版本应将账户标识、时区、并发锁和限额变更记录纳入审计。

### 10.4 约束与反循环设计

- 每个储备份额只能成功领取一次。
- 兑换成功后，系统需创建 CLIP 支出流水与对应 HAPW 状态记录。
- 储备价格应显著高于同一资产核销可获得的 CLIP 数量，以降低“兑换、核销、再兑换”的直接循环空间。
- 核销后的 HAPW 不应再次作为可兑换储备或资产行权对象。
- 手续费账户、储备盘点、定价权限和异常处理规则应公开。

当前原型以资产级别的 `exchangeAvailable` 状态阻止重复领取，并以幂等 `requestId` 阻止重试造成重复扣费。正式版本还需考虑并发兑换、链上确认失败、价格变更窗口和储备不足等情况。

---

## 11. AI 视频任务与内容记录

### 11.1 任务状态

完整的生产环境建议采用下列任务状态，而当前原型主要展示“已生成”结果：

```text
待校验 → 排队 → 生成中 → 审核中 → 已生成
                         └────────────→ 失败 / 退款
```

每个任务建议记录所用 HAPW、授权凭证、模型与版本、输入参数、时长、质量、额度消耗、CLIP 消耗、输出文件摘要、审核状态、失败原因与退款结果。

### 11.2 内容记录的定位

Clipli 可记录视频版本、渠道状态、审核结果和外部发布回执，以支持创作者复盘与内容运营。这些字段属于任务和运营记录，不参与 CLIP 金库分配计算，也不构成作品收益或价格保证。

正式接入外部内容数据时，应公开数据来源、采集频率、延迟、去重逻辑、异常识别、隐私处理、访问权限和留存期限。不同第三方平台的数据口径不得未经标准化便直接合并。

### 11.3 审核、申诉与退款

正式服务应针对三类事件分别定义规则：

- **模型或基础设施失败：** 队列未执行、模型报错、存储失败等；
- **内容审核失败：** 输出超出授权范围、违反平台规则或触发安全审核；
- **版权争议：** 权属证明被质疑、授权终止、素材范围变更或外部投诉。

这些事件的额度与 CLIP 处理不可由界面提示替代，应通过可查询的状态、退款标准、人工复核入口和时限明确说明。

---

## 12. 第三方平台与外部入口

### 12.1 平台选择器

在作品详情和资产行权页面中，用户可选择特定第三方平台。当前产品入口包含海文发、OpenSea、Foundation、SuperRare 和 Art Blocks。外部链接应在新窗口中安全打开，并使用 `noopener noreferrer` 等措施避免外部页面影响原窗口。

选择器的作用是让用户在离开 Clipli 前明确目标平台，而不是暗示所有平台对所有素材、用户或内容都开放。

### 12.2 资产行权记录

资产行权记录建议包含：

| 字段 | 说明 |
| --- | --- |
| HAPW 标识 | 被行权的资产 |
| 平台代码与名称 | 目标第三方平台 |
| 操作人和时间 | 便于审计与支持 |
| 签名状态 | 待签名、已提交、完成、失败或归档 |
| 外部标识 | 第三方资产、订单或回执标识（如有） |
| 规则快照 | 当时适用的网络、费用、授权和提示 |

行权记录仅说明平台内已创建某项操作，不说明第三方平台已经接纳、展示、上架、交易或完成交付。

### 12.3 适配器方向

HAPW 的资产事实全部来自外部平台，Clipli 只保存带来源与同步时间的只读快照。上游平台至少需要提供以下读取能力：

| 接口 | 用途 | 必要性 |
| --- | --- | --- |
| `GET /assets` | 分页读取 HAPW 资产与同步游标 | 必需 |
| `GET /assets/{assetId}` | 读取单个资产、状态和外部标识 | 必需 |
| `GET /assets/{assetId}/authorization` | 读取权利方、地域、渠道、期限与限制 | 必需 |
| `GET /assets/{assetId}/media` | 读取预览图、素材清单、哈希和媒体类型 | 必需 |
| `GET /assets/{assetId}/events` | 读取授权、冻结、核销或状态变更事件 | 必需 |
| `GET /works?assetId={assetId}` | 读取与 HAPW 关联的作品信息 | 必需 |
| `POST /webhooks/asset-events` | 主动通知资产变化，并附签名与事件 ID | 可选 |

每个资产快照必须含 `sourceCode`、`externalAssetId`、`sourceUrl`、`syncedAt` 和 `sourceUpdatedAt`。Clipli 对上游只执行读取，不回写外部权属；同步失败时保留上一份有效快照、标记其新鲜度并写入同步运行记录。上游响应应支持稳定游标、幂等事件 ID、HTTPS、服务端凭据、超时与速率限制。完整字段约束以 `docs/API.md` 和 `api/openapi.yaml` 为准。

作品展示与资产行权仍应为每个外部平台提供独立适配器，统一但不抹平其差异。适配字段可包括平台资产标识、支持网络、合约标准、上架状态、审核状态、下架原因、费用、回执、速率限制和可用地区。

第三方平台服务条款独立生效。Clipli 不替代其可能存在的身份、内容审核、交易结算、税务处理或法律责任判断。

---

## 13. 账本与可追溯性

### 13.1 独立实体与账本

Clipli 不应只维护一个余额，而应保留至少以下独立实体：

- HAPW 生命周期记录；
- HAPW 核销凭证；
- 单凭证与总账户的创作额度；
- CLIP 收入、支出和来源；
- CLIP 平台金库、分配规则与分配流水；
- AI 视频生成任务；
- 资产行权请求；
- 储备 HAPW 兑换记录；
- 外部平台与作品来源记录。
- HAPW 外部数据源、同步运行与快照新鲜度；
- 匿名会话与操作上下文。

### 13.2 引用链

核心引用链如下：

```text
HAPW
  → 核销凭证
  → 创作额度 + CLIP 发放
  → AI 视频生成任务
  → 内容表现与外部状态记录
```

并行路径如下：

```text
USDT ↔ 外部 DEX 池 ↔ CLIP
CLIP → 储备兑换 → HAPW
未核销 HAPW → 资产行权 → 第三方平台
```

通过引用而非混合状态，系统能够回答“这笔 CLIP 来自哪里”“哪次生成使用了哪份授权”“哪个外部操作对应哪一枚 HAPW”等问题。

### 13.3 幂等与审计

核销、生成、储备兑换、区域授权和资产行权等影响余额或状态的操作必须使用唯一 `requestId`。重复提交相同 `requestId` 时，系统应返回首次结果，而不是重复发放、重复扣费或创建多条请求。

每条关键流水应保留时间、操作者、输入参数、机制版本、计算结果、关联对象和状态变化。正式系统可将账本设计为追加式记录，禁止直接覆盖历史数值；修正应通过反向或更正流水完成。

---

## 14. 技术架构

### 14.1 当前原型

当前原型采用轻量架构：

- 浏览器单页应用，提供作品、资产、生成、机制、行权与关于页面；
- Go 标准库 REST 服务，处理读取、核销、生成、兑换、金库分配和行权请求；
- 内存演示数据，便于展示状态变化；
- 只读 HAPW 外部数据适配器与作品来源适配器，可接入外部平台 API；
- 集中的机制模块，覆盖额度、CLIP 发放、生成费用、兑换手续费和 DEX 快照公式；
- 单元测试、竞态检测与 OpenAPI 契约验证覆盖关键公式、守恒关系和数据适配。

这套原型不处理真实资产、真实私钥、真实链上签名或真实 AI 模型调用。

### 14.2 正式架构建议

正式系统建议拆分为以下层次：

```text
前端与本地化资源
        ↓
API 网关、匿名会话与操作授权服务
        ↓
授权 / HAPW / 核销 / 额度 / CLIP / 生成 / 行权服务
        ↓
不可变流水数据库、审计日志、对象存储与任务队列
        ↓
钱包与链上索引 | DEX 报价 | AI 模型 | 内容审核 | 第三方平台适配器
```

其中，账本与任务服务应支持幂等、事务、并发控制、重放防护、权限隔离和可观测性；生成服务应具备队列、回调、重试和退款状态；第三方适配器应能处理超时、速率限制、字段差异和回执延迟。

### 14.3 链上与链下数据边界

建议仅将适合公开验证的资产标识、核销状态、交易或行权证明、哈希和签名摘要放入链上或可验证层。版权文件、未公开授权条款、素材源文件、用户输入、生成媒体和隐私数据应保留在受控链下系统中。

CLIP 链上合约应保持最小化：一次性铸造固定供应，提供标准余额与转账能力，不配置公开增发、质押、收益或复杂治理模块。HAPW 的权利明细、CLIP 中心化分配规则、额度与生成任务保留在链下可审计账本中。链上记录可以增强状态验证，但不能替代版权合同、内容审查或司法处理。

---

## 15. 安全、隐私与内容治理

### 15.1 钱包与操作安全

Clipli 不应请求、存储或传输用户私钥与助记词。对需要签名的操作，界面应在钱包弹窗前展示资产、网络、费用、目标平台、授权范围和不可逆提示。

外部 URL 应执行协议校验与白名单控制，仅允许明确的 `https:` 或必要的 `http:` 地址。应用还应采用内容安全策略、禁止不受控嵌入、输入验证、速率限制和重放防护。

### 15.2 隐私与数据最小化

当前产品采用匿名演示会话，不要求真实姓名、证件或 KYC；`anonymous-demo` 只是进程内用户引用，钱包连接也是可选操作句柄，不构成真实身份认证。平台只收集完成授权、生成和支持所需的数据。匿名用户引用、可选钱包地址、内容输入和任务记录应按最小必要原则保存，并明确访问权限、加密措施、保存期限、删除或导出机制。

跨语言产品应确保关键确认、风险提示、错误信息和投诉入口在用户所选语言中准确呈现。

### 15.3 内容治理

AI 视频服务应设置可执行的内容治理流程，包括：

- 输入素材与授权范围核验；
- 输出内容自动与人工审核；
- 侵权、仿冒、违规内容的投诉与处理；
- 争议 HAPW、授权暂停和冻结机制；
- 下架、通知、复核与申诉时限；
- 对模型供应商、审核方和第三方平台的责任分配。

在这些流程正式形成前，Clipli 不应将原型展示理解为任何特定内容均可上线或跨平台发布。

---

## 16. 法律与合规

### 16.1 HAPW 与底层版权

HAPW 是平台记录的授权与操作资产，而非当然意义上的版权证书。正式法律文件应具体说明：HAPW 持有、转让、核销和行权分别授予或不授予哪些权利；涉及何种地域、渠道、期限、语言、模型、再许可和署名要求；权属争议、授权撤销和违约发生时的处理方式。

### 16.2 CLIP 的功能定位

CLIP 的设计定位是生成和生态服务中的功能性单位。不同司法辖区对代币、积分、兑换、市场推广、消费者保护、反洗钱、税务与会计的要求可能不同。项目在公开发行、交易、营销或扩大用户范围前，应取得相应司法辖区的专业意见。

### 16.3 钱包、DEX 与地区限制

Clipli 本身不执行真实身份验证或 KYC。钱包连接和外部 DEX 使用由用户与第三方直接完成；第三方可能基于其所在地和服务规则设置地区、制裁筛查、反欺诈、账户或网络费用要求。Clipli 只负责明确跳转目标、展示风险提示，不采集证件以替代第三方流程，也不承诺第三方服务可用。

### 16.4 用户生成内容与第三方条款

用户对其输入、输出、发布和外部平台操作承担相应责任。Clipli 应明确平台服务条款、版权投诉规则、内容审核政策、第三方平台跳转声明和责任限制。任何平台规则均不能排除法律强制要求或消费者保护义务。

---

## 17. 治理与参数变更

### 17.1 可治理参数

建议将以下参数视为版本化、可审计的配置，而非隐藏常量：

| 参数 | 当前原型值 | 说明 |
| --- | ---: | --- |
| 标准质量系数 | 1.0 | 用于计算创作额度 |
| 高质量系数 | 1.5 | 用于计算创作额度 |
| 核销 CLIP 发放率 | 0.40 / 额度 | 决定 `G` |
| 生成 CLIP 费率 | 0.20 / 额度 | 决定 `U` |
| 储备兑换手续费 | 5% | 决定 `T` 中的手续费 |
| HAPW 每日储备兑换上限 | 2 枚 / 账户 / 日 | 控制有限库存的兑换速度 |
| 支持时长 | 15 / 30 / 60 秒 | 当前原型的任务配置 |
| 支持平台清单 | 5 个入口 | 外部平台选择器范围 |

### 17.2 变更原则

1. 变更前应公开版本号、理由、生效时间、影响范围和历史对比。
2. 已核销凭证和已完成任务保留当时的参数快照，不追溯修改既有流水。
3. 涉及分配来源、费用、储备价格、合约权限或用户权利的重大变更，应经过更高等级的审阅与公告。
4. 任何正式治理机制的投票权、执行权、否决权和紧急暂停权都应在单独文件中公开。

当前原型尚未部署链上治理。所谓“公开治理”仅指未来应采用版本化披露与可审计决策流程，不代表已经存在任何链上投票或自治组织。

---

## 18. 商业模式

Clipli 的潜在收入来源以服务收费为中心，而非以 CLIP 升值、固定回购或内容表现回报作为承诺：

- AI 视频生成服务费用；
- 区域授权与本地化服务费用；
- 储备 HAPW 兑换手续费；
- 面向版权方、发行方和机构客户的 API、SaaS、素材管理与适配服务。

服务费用进入何种账户、用于何种运营成本、是否形成激励预算，均需要在正式商业与账本规则中披露。Clipli 不应以价格上涨、流动性增长、播放表现或固定收益作为获客或商业模式表述。

---

## 19. 路线图

### 阶段一：功能原型

- 演示素材与 HAPW；
- 核销后同时领取额度与 CLIP；
- 额度与 CLIP AI 视频生成；
- 六语界面、亮暗模式；
- DEX 演示快照、储备兑换和五个平台入口；
- 基础机制单元测试。
- HAPW 外部数据契约、同步状态和来源展示；
- 固定供应金库、中心化分配流水与匿名会话策略。

### 阶段二：封闭测试

- 将内存状态迁移到数据库与不可变流水；
- 接入测试网钱包、签名与链上状态索引；
- 引入真实任务队列、对象存储、模型回调和失败退款；
- 建立版权审核、申诉、冻结和内容治理流程；
- 为第三方平台建设状态适配器。

### 阶段三：受控上线

- 公开正式网络、最小化合约、精度、一次性铸造记录和平台金库地址；
- 配置可信 CLIP 流动性池及完整风险披露；
- 完成储备 HAPW 审计、定价策略和地区准入设计；
- 建立匿名会话风控、外部服务地区提示、用户支持和安全响应能力，不在 Clipli 内采集真实身份材料。

### 阶段四：生态扩展

- 接入更多作品来源 API、AI 模型和第三方平台；
- 为机构版权方提供素材包、授权模板、区域管理和报表工具；
- 发布受控开放 API 与开发者文档；
- 在验证安全、合规和用户体验后，逐步扩展支持网络与内容类型。

路线图是开发方向，不构成上线时间、功能交付或代币市场安排承诺。

---

## 20. 风险披露

使用 Clipli 相关功能前，用户应理解至少以下风险：

### 20.1 权利与授权风险

版权链条可能存在瑕疵、误陈述、授权范围不明、地区限制、期限届满、撤销、第三方权利主张或司法争议。HAPW 记录不能替代完整的法律审查。

### 20.2 AI 生成与内容风险

模型输出可能存在不确定性、质量不足、与授权范围不一致、侵权、误导、违规或被第三方平台拒绝的风险。任务失败、模型变化、审核延迟和下架均可能发生。

### 20.3 CLIP 与 DEX 风险

CLIP 可能面临价格波动、滑点、流动性不足、交易失败、合约漏洞、假冒代币、网络拥堵、钱包错误和外部 DEX 风险。演示池数据不代表实际市场。

### 20.4 储备兑换风险

储备 HAPW 数量有限，价格可能变化，兑换可能因余额不足、资产状态变化、并发请求或规则调整而无法完成。5% 手续费为当前原型参数，正式参数须以届时披露为准。

### 20.5 跨平台风险

第三方平台可能修改服务条款、支持网络、审核标准、费用、可用地区或下架规则。Clipli 无法控制其账户状态、成交、展示、上架和外部数据准确性。

### 20.6 安全与运营风险

任何软件系统都可能面临漏洞、权限配置错误、接口故障、数据同步延迟、服务中断、供应商失效、社会工程攻击和不可抗力。用户应自行保护钱包和账户安全。

### 20.7 法律、税务与语言风险

不同地区的法律、税务、消费者保护、数字资产监管和版权规则可能变化。多语言文本若存在歧义，应以适用法律、正式合同与对应司法辖区的权威版本为准。

---

## 21. 附录

### 21.1 术语表

| 术语 | 含义 |
| --- | --- |
| Clipli | 用于记录版权素材、授权、生成、费用与外部行权的工作平台 |
| HAPW | 由版权持有方授权、对应具体素材及使用边界的核心资产 |
| CLIP | 用于生成与生态服务的功能性代币；不代表版权或固定收益 |
| 平台金库 | 保存一次性铸造 CLIP，并按版本化中心化规则分配或回收费用的账户 |
| 分配流水 | 记录 CLIP 从平台金库到用户账本的规则、请求、数额和时间 |
| 创作额度 | 从 HAPW 核销中获得、与授权凭证绑定的计算配额 |
| 核销 | 不可重复地将 HAPW 转为生成授权凭证、额度和 CLIP 的操作 |
| 授权凭证 | 保存核销时的 HAPW、权利快照、额度和状态的记录 |
| 资产行权 | 将未核销 HAPW 的可执行操作关联到外部平台并创建签名请求 |
| DEX | 去中心化交易所或外部链上兑换界面 |
| 流动性池 | 为代币交换提供储备的池；其状态影响报价与滑点 |
| 储备 HAPW | 平台预先列出的、可用 CLIP 加手续费兑换的未领取 HAPW |
| 内容记录 | 记录版本、审核、渠道状态与外部回执，用于复盘，不作为 CLIP 分配依据 |

### 21.2 当前原型参数表

| 参数 | 当前原型值 | 正式变更方式 |
| --- | ---: | --- |
| 标准质量系数 | 1.0 | 版本化披露与审阅 |
| 高质量系数 | 1.5 | 版本化披露与审阅 |
| 核销 CLIP 发放率 | 0.40 / 额度 | 公开参数变更流程 |
| 生成 CLIP 费率 | 0.20 / 额度 | 公开参数变更流程 |
| 储备兑换手续费 | 5% | 公开参数变更流程 |
| HAPW 每日储备兑换上限 | 2 枚 / 账户 / 日 | 公开参数变更流程 |
| 演示 CLIP 储备 | 250,000 CLIP | 仅用于原型快照 |
| 演示 USDT 储备 | 25,000 USDT | 仅用于原型快照 |
| 演示参考比例 | 1 USDT 约等于 10 CLIP | 不代表实际成交 |
| 当前支持时长 | 15 / 30 / 60 秒 | 由生成服务能力决定 |
| 一次性铸造总量 | 10,000,000 CLIP | 正式部署前确认并公开链上记录 |
| 平台金库演示余额 | 9,746,540 CLIP | 随分配与费用回流变化 |
| 用户与业务账本演示余额 | 3,460 CLIP | 与金库、DEX 配置共同守恒 |

### 21.3 正式发布前待补材料

1. HAPW 授权合同模板、元数据 schema 和权属核验流程。
2. 正式网络、合约地址、合约精度、一次性铸造与金库权限的独立审计报告。
3. CLIP 初始分配记录、流动性池地址、初始资金来源和风险模型。
4. 储备 HAPW 完整清单、来源、定价方法、手续费归属和审计频率。
5. AI 模型服务等级、数据处理说明、审核政策、失败退款与申诉规则。
6. 五类第三方平台的适配字段、服务条款摘要和责任边界。
7. 目标司法辖区的代币、版权、数据、消费者保护、税务及外部服务准入法律意见。
8. 面向用户的正式服务条款、隐私政策、风险提示和多语言权威版本规则。

---

**文档结束。** 本白皮书应与产品界面、合约说明、授权合同、风险提示和版本记录共同阅读。
