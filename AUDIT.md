# Clipli 当前审查记录

> 审查日期：2026-08-18
> 当前机制版本：Clipli V1.1

## 结论

项目主线已统一为：Clipli（Create、Click、Grow）连接 IP 授权与 AI 创作；版权方授权 HAPW，用户核销后同时获得创作额度与 CLIP，生成视频同时消耗两者。CLIP 可从外部 DEX 获取，也可按公开价格加 5% 手续费兑换平台有限储备 HAPW。

## 本轮完成

- 删除旧版收益分账与内容表现发放逻辑，生成记录只保留任务、版本和运营状态。
- 增加 `0.40 CLIP / 核销额度` 发放公式与 `0.20 CLIP / 创作额度` 消耗公式。
- DEX 卡披露参考汇率、CLIP/USDT 双边储备、名义流动性和快照时间。
- 明确 HAPW 资产事实来自外部平台，并增加资产、授权、媒体、事件、作品和可选 webhook API 契约。
- 增加外部源同步快照、失败保留上一份有效数据和同步运行记录。
- 增加 CLIP→HAPW 储备兑换、5% 手续费、一次性领取和幂等流水。
- CLIP 改为一次性铸造后存入平台金库；分配、回流和用户账本满足 `M = T + L + B` 守恒关系。
- 增加平台金库、分配规则、分配流水和匿名会话 API；Clipli 不做真实身份验证或 KYC。
- 后端从 Node.js 迁移为 Go 标准库 HTTP 服务，并通过 `go test -race` 与 `go vet`。
- 用户可见的资产转移统一改为资产行权，目标从固定海文发扩展为五个第三方平台。
- 作品详情增加第三方平台选择器；关于页增加 8 项机制 Q&A。
- 中、英、西、日、法、韩六种语言同步更新。

## 风险边界

- 当前汇率、池储备、HAPW 价格和任务均为演示数据，不是实时报价。
- Clipli 不托管私钥，不替代版权合同、链上确认或第三方平台审核。
- CLIP 固定总量一次性铸造，当前演示总量为 10,000,000；平台金库、DEX 配置与用户账本始终守恒，分配只能从金库余额划拨。
- 反向兑换只从有限储备中领取，不能重复；价格显著高于同一资产核销发放量，避免直接循环套利。
- 正式上线前需补充网络、合约、精度、流动性来源、储备管理与司法辖区准入说明。

## 下一阶段

1. 将内存状态迁移到数据库，分别保存授权、核销、额度、CLIP 金库与分配、生成、行权和储备兑换流水。
2. 接入真实模型时增加任务队列、失败退款、内容审核和版权申诉。
3. 为外部 HAPW 来源和五个第三方平台建立适配器，读取授权、媒体、事件、作品、上架状态和平台资产标识。
4. 对 DEX 数据使用可信报价源，并明确快照延迟、滑点和池地址。
